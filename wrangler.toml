# Cloudflare Pages
# Build: npm run build | Output dir: dist
# Deploy: npx wrangler pages deploy dist --project-name=9xmhits
# Or connect the GitHub repo in Cloudflare Dashboard → Workers & Pages

name = "9xmhits"
compatibility_date = "2026-08-14"
pages_build_output_dir = "dist"
